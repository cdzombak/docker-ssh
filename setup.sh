#!/bin/bash

# One-time setup at build time.

# NEWUSERNAME=anewusername
# NEWUSERPASS=foobarbaz123

# useradd -d /a-shared-directory/ -u 1000 -g users -s /usr/bin/bash "$NEWUSERNAME"
# echo "$NEWUSERNAME:$NEWUSERPASS" | chpasswd
